<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier</title>
    <style>
        * {
            border-collapse: collapse;
        }

        html, body {
            margin: 0;
            border: 0;
            padding: 0;
        }

        body{
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        header{
            display: flex;
            flex-direction: row;
            width: 100%;
            background-color: #000000;
        }

        header > a{
            padding: 10px;
            text-decoration: none;
            color: #FFFFFF;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
            width: 100%;
            margin: 10px;
        }

        form > input, form > textarea{
            width: 320px;
            height: 25px;
            padding-left: 10px;
            margin-bottom: 20px;
        }

        .edit {
            width: 100px;
            height: 25px;
            padding-left: 0px;
            margin-bottom: 0px;
            background-color: #F2E3B3;
        }

        .delete {
            width: 100px;
            height: 25px;
            padding-left: 0px;
            margin-bottom: 0px;
            background-color: #BF7C63;
        }
    </style>
</head>
<body>
    <header id="header">
        <a href="/">Home</a>
        <a href="/inscription">Inscription</a>
        <a href="/connexion">Connexion</a>
    </header>
    <h1>Modifier la Salle <%= data.roomname %></h1>
    <form action="/api/modifiersalle/<%= data._id  %>?_method=PUT" method="post">
        <input type="hidden" name="_method" value="PUT">

        <label for="idRoomName">Nom Salle</label>
        <input type="text" name="roomname" id="idRoomName" value="<%= data.roomname %>">

        <label for="idRoomSubName">Propriétaire</label>
        <input type="text" name="roomsubname" id="idRoomSubName" value="<%= data.roomsubname %>">

        <label for="idNombreTable">Nombre de Tables</label>
        <input type="number" name="nombretable" id="idNombreTable" value="<%= data.nombretable %>">

        <label for="idNombreSiegeTable">Nombre de Sièges par Table</label>
        <input type="number" name="nombresiegetable" id="idNombreSiegeTable" value="<%= data.nombresiegetable %>">

        <input type="submit" value="Valider" class="edit">
    </form>
</body>
</html>